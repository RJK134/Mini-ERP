// Re-exports for places that need enum values without pulling the full client.
export {
  UserRole,
  InboundSource,
  InboundStatus,
  CaseStatus,
  TaskStatus,
  Priority,
  DraftType,
  ActivityType,
} from "@prisma/client";
